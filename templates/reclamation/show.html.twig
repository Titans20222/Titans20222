{% extends 'base.html.twig' %}

{% block content %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Titre</th>
                <td>{{ reclamation.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.date }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamation.type }}</td>
            </tr>
        </tbody>
    </table>

    <center><a href="{{ path('reclamation_index') }}"><button>back to list</button></a></center>
    <br><br>
    <center>   <a href="{{ path('reclamation_edit', {'id': reclamation.id}) }}"><button>edit</button></a></center>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
    {% endblock %}
