
{% extends 'base.html.twig' %}
{% block content  %}
   

    {% block body %}
<br>
<br>
<br>
<br>
    <center>
        <h1>Commande</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Adresselivraison  </th>
                <td>{{ commande.adresselivraison  }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ commande.ville  }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ commande.Numc }}</td>
            </tr>
            <tr>
                <th>Prix total</th>
                <td>{{ commande.prix_total }}</td>
            </tr>
            </tbody>
        </table>
    </center>
        <table>
<tbody>
<tr>
        <a href="{{ path('commande_edit', {'id': commande.id}) }}" class="btn btn-success">edit</a>
</tr>
&emsp;
<tr>
        {{ include('commande/_delete_form.html.twig') }}
</tr>
&emsp;
<tr>
 <a href="{{ path('Envoyer_Mail') }}" class="btn btn-info btn-icon-text" >Reclamer</a>
 </tr>
        </tbody>
        </table>
        
        <center>
        <a href="{{ path('commande_index') }}">back to list</a>
        </center>
    {% endblock %}
{% endblock %}

