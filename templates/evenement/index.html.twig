{% extends 'base.html.twig' %}



{% block content%}

{% block title %}{% endblock %}

{% block body %}
<br>
<br>
<br>
<br>
    <h1>Evenements</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomEvenement</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evenement in evenements %}
            <tr>
                <td>{{ evenement.id }}</td>
                <td>{{ evenement.nomEvenement }}</td>
                <td>{{ evenement.date ? evenement.date|date('Y-m-d') : '' }}</td>
                <td>

                    <a href="{{ path('evenement_show', {'id': evenement.id}) }}">show</a>
                    <a href="{{ path('evenement_edit', {'id': evenement.id}) }}">edit</a>
                </td>
            </tr>
          
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        
        {% endfor %}
         <tr>
            <td><a href="{{ path('evenement_new') }}">Create new</a> </td>
            <td><a href="{{ path('reservation_new') }}">reserver</a></td>
           </td>
          <td>{{ knp_pagination_render(evenements) }}</td>
            
            </tr>
        </tbody>
         

    </table>

  
 
{% endblock %}
{% endblock %}
