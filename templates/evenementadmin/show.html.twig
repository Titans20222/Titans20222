{% extends 'adminT.html.twig' %}
{% block content  %}
{% block title %}Evenement{% endblock %}

{% block body %}
<br>
<br>
<br>
<br>

    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>NomEvenement</th>
                <td>{{ evenement.nomEvenement }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ evenement.date ? evenement.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evenement_indexadmin') }}">back to list</a>

    <a href="{{ path('evenement_editadmin', {'id': evenement.id}) }}">edit</a>

    {{ include('evenementadmin/_delete_form.html.twig') }}
      <a href="{{ path('test', {'id': evenement.id}) }}">voir stat</a>
{% endblock %}
{% endblock %}
